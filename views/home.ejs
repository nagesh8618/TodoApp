<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/Home.css">
</head>
<body>
    <header>
        <nav>
            <div class="logo">LOGO</div>
            <ul>
                <li  class="welcome-message">Welcome <%= name%></li>
                <li id="logout"><a href="/api/v1/user/logout">Logout</a></li>
            </ul>
        </nav>
    </header>
  <main>
    <div class="input_container">
        <h1>Todo</h1>
        <form action="/api/v1/todo" method="post">
            <input type="text" name="name">
            <button type="submit">Create</button>
        </form>
    </div>
    <div class="todo_list">
        <h2>List of todos</h2>
        <table>
            <tr>
                <th>Todo</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
            <% todos.forEach(todo=>{ %>
                <tr>
                    <td><%= todo.name%></td>
                    <td id="update"><a href="/api/v1/todo/<%= todo._id%>">Update</a></td>
                    <td><form action="/api/v1/todo/<%= todo._id%>?_method=DELETE" method="post">
                        <button id="del">Delete</button>
                    </form></td>
                </tr>
            <%})%>
        </table>
    </div>
  </main>
</body>
</html>